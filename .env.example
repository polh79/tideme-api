# ================================
# TIDEME API - VARIABLES D'ENVIRONNEMENT
# ================================

# StormGlass API Key (OBLIGATOIRE)
# Obtenir une clé gratuite sur https://stormglass.io
# Plan gratuit : 10 calls/jour (suffisant pour 3 ports × 2 refresh/jour = 6 calls)
STORMGLASS_API_KEY=your-stormglass-api-key-here

# Upstash Redis (OPTIONNEL - utilise cache mémoire si absent)
# Si non configuré, l'API utilise automatiquement un cache en mémoire
# Upstash gratuit : https://console.upstash.com
# Recommandé pour la production (cache persistant)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Vercel Cron Secret (OBLIGATOIRE pour le cron job)
# Générer un secret aléatoire pour sécuriser l'endpoint /api/cron/refresh
# Exemple : tideme-cron-secret-2025-xyz
CRON_SECRET=change-me-to-random-secret

# ================================
# NOTES
# ================================

# 1. Pour le développement local :
#    - Copier ce fichier en .env.local
#    - Remplir STORMGLASS_API_KEY et CRON_SECRET
#    - Laisser UPSTASH vide (cache mémoire sera utilisé)

# 2. Pour le déploiement Vercel :
#    - Configurer ces variables dans Settings → Environment Variables
#    - UPSTASH optionnel mais recommandé pour la production
